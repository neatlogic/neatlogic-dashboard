{
  "moduleId": "dashboard",
  "name": "dashboard_default",
  "label": "仪表板默认表",
  "description": "仪表板默认表 可用于仪表板相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "dashboard_id",
      "type": "bigint",
      "description": "仪表板唯一标识，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "user_uuid",
      "type": "varchar(32)",
      "description": "仪表板用户，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "type",
      "type": "enum('custom','system')",
      "description": "仪表板类型，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "dashboard_id",
        "user_uuid",
        "type"
      ],
      "description": "主键索引，覆盖 dashboard_id、user_uuid、type 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "idx_uuid_userid_type",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "dashboard_id",
        "user_uuid",
        "type"
      ],
      "description": "普通索引，覆盖 dashboard_id、user_uuid、type 字段，适合用于过滤、关联或排序查询。"
    }
  ]
}
