{
  "moduleId": "dashboard",
  "name": "dashboard_visitcounter",
  "label": "Dashboard Access Statistics Table",
  "description": "The dashboard access statistics table is used for related queries for dashboard-related reports.",
  "fields": [
    {
      "name": "dashboard_id",
      "type": "bigint",
      "description": "Dashboard ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "user_uuid",
      "type": "char(32)",
      "description": "User_uuid, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "visit_count",
      "type": "int",
      "description": "Access counts."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "dashboard_id",
        "user_uuid"
      ],
      "description": "Primary key index covering the dashboard_id, user_uuid fields, suitable for uniquely identifying records and queries that join the primary table."
    },
    {
      "name": "idx_user_uuid",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "user_uuid"
      ],
      "description": "Standard index covering the user_uuid field, suitable for filtering, join, and sort queries."
    }
  ]
}
