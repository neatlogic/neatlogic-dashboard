{
  "moduleId": "dashboard",
  "name": "dashboard_userdefault",
  "label": "用户默认仪表板表",
  "description": "用户默认仪表板表 可用于仪表板相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "dashboard_id",
      "type": "bigint",
      "description": "仪表板唯一标识，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "user_uuid",
      "type": "char(32)",
      "description": "用户uuid，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "dashboard_id",
        "user_uuid"
      ],
      "description": "主键索引，覆盖 dashboard_id、user_uuid 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "uk_user_uuid",
      "type": "BTREE",
      "unique": true,
      "primary": false,
      "columns": [
        "user_uuid"
      ],
      "description": "唯一索引，覆盖 user_uuid 字段，适合用于精确检索、唯一性判断和去重查询。"
    }
  ]
}
